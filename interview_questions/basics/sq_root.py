import math

n = int(input('enter num? '))
sq_root = math.sqrt(n)
print(sq_root)

# ==========================================================

n = int(input('enter num? '))
sq_rt = n ** 0.5

print(sq_rt)
